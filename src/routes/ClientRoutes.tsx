import { Routes, Route } from 'react-router-dom'
import React from 'react'

const ClientRoutes = () => {
  return <Routes>{/* Bọc tất cả routes trong ClientLayout */}</Routes>
}

export default ClientRoutes
